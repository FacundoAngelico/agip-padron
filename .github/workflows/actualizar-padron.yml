name: Actualización AGIP demo

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout con historial
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Crear archivo falso database.db
        run: echo "demo $(date)" > database.db

      - name: Configurar Git y subir cambios
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull origin main --rebase
          git add database.db
          git commit -m "🧪 Push test $(date +'%Y-%m-%d %H:%M')" || echo "Nada para commitear"
          git push origin HEAD:main
