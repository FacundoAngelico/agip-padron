name: Actualización AGIP con LFS

on:
  schedule:
    - cron: '0 6 1 * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout con LFS
        uses: actions/checkout@v4
        with:
          lfs: true  # ¡Importante para LFS!
          fetch-depth: 0  # Trae todo el historial

      # [Tus pasos de Python aquí...]

      - name: Configurar Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sincronizar y pushear cambios
        run: |
          git pull --rebase origin main
          git add database.db
          git commit -m "🔄 Actualización automática $(date +'%Y-%m-%d')" || echo "Nada para commitear"
          git push origin HEAD:main
